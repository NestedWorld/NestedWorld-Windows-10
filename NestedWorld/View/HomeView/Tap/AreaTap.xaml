<UserControl
    x:Class="NestedWorld.View.HomeView.Tap.AreaTap"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View.HomeView.Tap"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Polygon HorizontalAlignment="Center" Points="5,0 10,10, 0,10" Stroke="{StaticResource DarkApplicationbackgroundBrush}" Fill="{StaticResource DarkApplicationbackgroundBrush}" Grid.Column="2" />
        </Grid>
        <Grid Margin="10, 0, 10, 10" Grid.Row="1" Background="{StaticResource DarkApplicationbackgroundBrush}" BorderBrush="{StaticResource DarkApplicationbackgroundBrush}">
            <TextBlock Text="Area"/>
            <TextBlock Text="Test" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

    </Grid>
</UserControl>

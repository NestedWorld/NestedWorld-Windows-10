<UserControl
    x:Class="NestedWorld.View.BattleViews.AnnimationCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View.BattleViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:NestedWorld.UI"
    mc:Ignorable="d"
   >
    <UserControl.Resources>


    </UserControl.Resources>

    <Grid x:Name="rootGrid">
        <Grid.Background>
            <SolidColorBrush Color="#E523232C"/>
        </Grid.Background>
        <!--ui:BlurBackground BlurAmount="49" BlurRadius="158" BackgroundColorBlur="{StaticResource BleuTwoBrush}"/-->
        <local:InformationBattleView
            x:Name="informationBatteView"
            x:FieldModifier="public" RenderTransformOrigin="0.5,0.5"/>
    </Grid>
</UserControl>

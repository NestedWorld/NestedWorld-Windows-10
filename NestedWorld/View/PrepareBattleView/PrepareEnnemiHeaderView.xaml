<UserControl
    x:Class="NestedWorld.View.PrepareBattleView.PrepareEnnemiHeaderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View.PrepareBattleView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="400">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="90"/>
        </Grid.ColumnDefinitions>

        <Ellipse Grid.Column="1" Height="80" Width="80" Stroke="{StaticResource BackrgoundBrush}">
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding Path=Image}"/>
            </Ellipse.Fill>
        </Ellipse>
    </Grid>
</UserControl>
